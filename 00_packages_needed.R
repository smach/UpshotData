if(!require("pacman")){
 install.packages("pacman")
} 

pacman::p_load(
  sf,
  geojsonsf,
  dplyr,
  tidycensus,
  tmap,
  htmlwidgets,
  tmap,
  leaflet,
  leaflet.extras,
  htmltools,
  glue,
  scales
)


